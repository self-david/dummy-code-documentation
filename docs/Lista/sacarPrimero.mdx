---
sidebar_position: 16
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# sacarPrimero
Este método modifica el arreglo original al eliminar el primer elemento, reduciendo la longitud del arreglo en 1.

## Ejemplo📝

```dummy
// Crear un arreglo de ejemplo
var frutas = ['manzana', 'pera', 'naranja'];

// Eliminar el primer elemento del arreglo usando sacarPrimero
var primeraFruta = frutas.sacarPrimero();

imprimir(primeraFruta);
```
Salida:

```dummy
// ['pera', 'naranja']
```
