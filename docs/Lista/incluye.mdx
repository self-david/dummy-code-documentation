---
sidebar_position: 9
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# incluye

Se utiliza para verificar si un arreglo (también conocido como lista) contiene un elemento específico. Retorna verdadero si el elemento está presente en el arreglo y falso si no lo está. 

## Ejemplo📝

```dummy
// Crear un arreglo de ejemplo
var frutas = ['manzana', 'pera', 'naranja', 'uva'];

// Verificar si el arreglo contiene 'naranja'
var contieneNaranja = frutas.incluye('naranja');
imprimir(contieneNaranja);
```
Salida:

```dummy
// verdadero
```
Este método no modifica el arreglo original y toma como argumento el elemento que deseas buscar.