---
sidebar_position: 16
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# invertir
Se utiliza para invertir el orden de los elementos en un arreglo (también conocido como lista). Este método modifica el arreglo original al cambiar el orden de los elementos.

## Ejemplo📝

```dummy
// Crear un arreglo de ejemplo
var numeros = [1, 2, 3, 4, 5];

// Invertir el orden de los elementos en el arreglo usando invertir
var deReversa = numeros.invertir();

imprimir(deReversa);
```
Salida:

```dummy
// [5, 4, 3, 2, 1]
```