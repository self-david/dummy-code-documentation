---
sidebar_position: 11
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# aPrecision
Formatea un número con una cantidad específica de dígitos significativos y lo devuelve como una cadena de texto.

## Ejemplo📝

```dummy
var numero = 123.45678;
var digitosSignificativos = 5;

var resultado = numero.aPrecision(digitosSignificativos);

imprimir("El número formateado es: " + resultado);
```
Salida:

```dummy
//"123.46"
```
Nuevamente, ten en cuenta que el resultado es una cadena de texto, no un número.