---
sidebar_position: 10
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# decimales
Se utiliza para formatear un número con una cantidad específica de decimales y devolverlo como una cadena de texto.

## Ejemplo📝

```dummy
var numero = 123.45678;
var decimal = 2;

var resultado = numero.decimales(decimal);

imprimir("El número formateado es: " + resultado);
```
Salida:

```dummy
//"123.46"
```
Recuerda que el resultado es una cadena de texto, no un número.