---
sidebar_position: 8
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# signo
Se utiliza para determinar el signo de un número. Devuelve 1 si el número es positivo, -1 si es negativo y 0 si es cero.

## Ejemplo📝

```dummy
var numeroPositivo = 7;
var numeroNegativo = -3;
var numeroCero = 0;

var signoPositivo = Mate.signo(numeroPositivo);
var signoNegativo = Mate.signo(numeroNegativo);
var signoCero = Mate.signo(numeroCero);

imprimir("Número positivo: " + signoPositivo);
imprimir("Número negativo: " + signoNegativo);
imprimir("Número cero: " + signoCero);
```

Salida:

```dummy
//Número positivo: 1
//Número negativo: -1
//Número cero: 0
```