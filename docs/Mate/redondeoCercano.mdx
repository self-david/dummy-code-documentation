---
sidebar_position: 7
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# redondeoCercano
Redondea un número decimal al entero más cercano.

## Ejemplo📝

```mdx-code-block
<BrowserWindow title=''>
<Tabs>
<TabItem value="var1" label="Variante 1">
```
```dummy
var numeroDecimal = 4.7;
var numeroRedondeado = Mate.redondeoCercano(numeroDecimal);

imprimir("Número original: " + numeroDecimal);
imprimir("Número redondeado: " + numeroRedondeado);
```

Salida:

```dummy
// Número original: 4.7
// Número redondeado: 5
```

```mdx-code-block
</TabItem>
<TabItem value="var2" label="Variante 2">
```
```dummy
var numero = 10.3;

var redondeadoHaciaAbajo = Mate.redondeoCercano(numero);

imprimir("Número original:", numero);
imprimir("Número redondeado hacia abajo:", redondeadoHaciaAbajo);
```
Salida:
```dummy
// Número original: 10.3
// Número redondeado hacia abajo: 10
```

```mdx-code-block
</TabItem>
</Tabs>
</BrowserWindow>
```