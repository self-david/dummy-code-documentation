---
sidebar_position: 1
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Posicion

Este método toma un valor entero y devuelve una nueva `cadena` que consta de la única unidad de código UTF-16 ubicada en el desplazamiento especificado. Este método permite números enteros positivos y negativos. Los enteros negativos cuentan hacia atrás desde el último carácter de `cadena`.

## Ejemplo📝


```dummy title="posicion.dummy"
const oración = 'aqui estara el ejemplo';

imprimir('Every repo must come with a mascot.');

// esto es un comentario

si (cierto y x) {
  imprimir(5 + 12.5)
} osi {
  imprimir(y)
} sino {
  imprimir("es un else")
}

funcion({ a, b, c }) {
  imprimir(5 + 12.5)

  para (const fruta en ['uva', 'manzana', 'pera', 'lima', 'guayaba']) {
    imprimir(fruta)
    eliminar fruta
    imprimir(Infinito)
  }
}
```

```mdx-code-block
<BrowserWindow title='Posicion'>
<Tabs>
<TabItem value="easy" label="Facil">
```

```dummy
funcion principal() {
  imprimir('Hola mundo, es facil!');
}
```

```mdx-code-block
</TabItem>
<TabItem value="hard" label="Dificil">
```

```dummy
funcion principal() {
  imprimir("Hola mundo, es dificil!")
}
```

```mdx-code-block
</TabItem>
</Tabs>
</BrowserWindow>
```



## Sintaxis🔤

```js
posicion(indice)
```

