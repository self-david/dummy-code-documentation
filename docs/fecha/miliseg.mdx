---
sidebar_position: 5
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# miliseg
Con este se obtiene los milisegundos de una instancia de un objeto Fecha. Devuelve un número entero que representa los milisegundos en la fecha y hora específica.

## Ejemplo📝

```dummy
var fecha = nueva Fecha();

var milisegundos = fecha.miliseg();

imprimir("Milisegundos actuales:", milisegundos);
```
Salida:

```dummy
//Milisegundos actuales: 487
```