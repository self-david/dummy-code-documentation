---
sidebar_position: 13
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# nulo
El valor nulo se utiliza para indicar que una variable o una referencia no apunta a ningún objeto o valor válido. 

## Ejemplo📝

```dummy
var variableNula = nulo; // Declaración de una variable con el valor nulo

funcion ejemploNulo() {
  imprimir("Ejemplo de 'nulo' en dummycode:");

  si (variableNula === nulo) {
    imprimir("La variable es nula.");
  } sino {
    imprimir("La variable no es nula.");
  }
}

ejemploNulo();

```
Salida:

```dummy
// Ejemplo de 'nulo' en dummycode:
// La variable es nula.
```