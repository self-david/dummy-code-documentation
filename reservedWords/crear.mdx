---
sidebar_position: 9
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# crear
se utiliza en algunos lenguajes de programación para crear nuevas instancias de objetos o clases. 

## Ejemplo📝

```dummy
function Persona(nombre) {
  var persona = {}; // Creamos un objeto vacío

  // Agregamos una propiedad "nombre" al objeto
  persona.nombre = nombre;

  // Devolvemos el objeto creado
  regresa persona;
}

var persona1 = crear Persona("Juan");

imprimir(persona1.nombre);
```
Salida:

```dummy
// Juan
```
