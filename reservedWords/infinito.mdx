---
sidebar_position: 16
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# infinito
"Infinito" se utiliza para representar valores que son mayores que cualquier número finito y puede ser útil en cálculos matemáticos o en situaciones en las que necesitas indicar que no hay un límite superior para un valor numérico.
## Ejemplo📝

```dummy
var valorInfinito = Infinito; // Declaración de una variable con el valor "Infinito"

funcion ejemploInfinito() {
  imprimir("Ejemplo de 'Infinito' en dummycode:");

  si (valorInfinito === Infinito) {
    imprimir("El valor es infinito.");
  } más {
    imprimir("El valor no es infinito.");
  }
}

ejemploInfinito();

```
Salida:

```dummy
// Ejemplo de 'Infinito' en dummycode:
// El valor es infinito.
```