---
sidebar_position: 17
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NeN
NeN se utiliza para representar valores que no pueden ser tratados como números en cálculos matemáticos y es útil para detectar operaciones que producen resultados no numéricos.

## Ejemplo📝

```dummy
var x = Numero('Hola');
var y = 5;
var resultado = x + y;
imprimir("El resultado es: " + resultado);
```
Salida:

```dummy
// El resultado es: NeN
```