---
sidebar_position: 2
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# const
Una vez que se le asigna un valor a una variable constante, ese valor no puede cambiarse durante la ejecución del programa. Las variables declaradas con "const" son útiles cuando se necesita asegurar que un valor no cambie accidentalmente a lo largo del programa.

## Ejemplo📝
```dummy
const numeros = [1, 2, 3, 4, 5]; // Declaración de una constante para una lista

imprimir("Lista de números: " + numeros);
```
Salida:

```dummy
// [1, 2, 3, 4, 5]
```
