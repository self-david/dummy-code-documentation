---
sidebar_position: 4
---

# Numero
Se utiliza para representar valores numéricos, ya sean enteros o números de punto flotante (decimales). Los números se utilizan en una variedad de operaciones matemáticas y cálculos en programación. 

Aquí tienes algunas características clave del tipo de dato "Número":

## Enteros y Decimales: 
Los números pueden ser enteros (números sin parte decimal) o decimales (números con una parte decimal).

## Operaciones Matemáticas: 
Los números se utilizan en una amplia gama de operaciones matemáticas, como suma (+), resta (-), multiplicación (*), división (/), y más.

## Precisión Limitada: 
Los números de punto flotante tienen una precisión limitada debido a la representación binaria interna. Esto puede dar lugar a problemas de precisión en cálculos que involucran decimales.

## Variables y Asignación: 
Los números se pueden asignar a variables y utilizar en expresiones. Por ejemplo, var x = 5; asigna el número 5 a la variable x.

## Comparaciones: 
Los números se pueden comparar utilizando operadores de comparación, como <, >, <=, >=, ==, y !=, para evaluar condiciones.

## Constantes: 
Se pueden definir constantes numéricas para valores que no cambian durante la ejecución del programa.

## Conversión de Tipos: 
Es común convertir números a otros tipos de datos, como cadenas de texto, cuando se necesita representar números en formato de texto.

## Funciones Matemáticas: 
Se proporcionan funciones matemáticas incorporadas para realizar operaciones más avanzadas, como exponenciación, raíz cuadrada, seno, coseno, etc.

## Ejemplo📝

```dummy
var entero = 42; // Número entero
var decimal = 3.14; // Número decimal
var resultado = entero + decimal; // Operación de suma

imprimir("El resultado de la suma es" + resultado);

si (resultado > 50) {
  imprimir("El resultado es mayor que 50.");
} sino {
  imprimir("El resultado es menor o igual a 50.");
}
```
Salida:

```dummy
// 45.14
// El resultado es menor o igual a 50.
```
