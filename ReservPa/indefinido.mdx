---
sidebar_position: 15
---

import BrowserWindow from '@site/src/components/BrowserWindow';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# indefinido
El valor "indefinido" se utiliza para indicar que una variable o referencia no tiene un valor asignado y es especialmente útil para verificar si una variable ha sido inicializada o definida previamente.

## Ejemplo📝

```dummy
var variableIndefinida = indefinido; // Declaración de una variable con el valor "indefinido"

funcion ejemploIndefinido() {
  imprimir("Ejemplo de 'indefinido' en dummycode:");

  si (variableIndefinida === indefinido) {
    imprimir("La variable no tiene un valor definido.");
  } sino {
    imprimir("La variable tiene un valor definido.");
  }
}

ejemploIndefinido();
```
Salida:

```dummy
//Ejemplo de 'indefinido' en dummycode:
//La variable no tiene un valor definido.
```